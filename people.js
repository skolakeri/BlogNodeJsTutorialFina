const people = ['yoshi', 'ryu', 'chun-li', 'mario'];
const ages = [20,25,30,35];

console.log(people);

// Used to allow manual exporting, set it equal to something. Hello will be returned to xyz in moduels
module.exports = {
    people,ages
    // under the hood people: people


};